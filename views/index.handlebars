<h1 class="text-center mb-3">{{ title }}</h1>
<h4>Current Members</h4>

<ul class="list-group">
    {{#each members}}
        <li class="list-group-item">
            <strong>{{ this.name }}</strong> - <em>{{ this.email }}</em>
        </li>
    {{/each}}
</ul>
